# Example content metadata

Reference JSON for local testing. Upload real audio to **Studio → Storage → bucket `content`**
using the layout in [content-layout.md](../content-layout.md).

These files are not scanned by the indexer — they document the expected on-disk shape only.
