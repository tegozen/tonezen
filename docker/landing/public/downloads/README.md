# Tonezen downloads

Place public app release files in this directory. The landing page links to these exact names:

- `tonezen-android.apk`
- `tonezen-windows.exe`
- `tonezen-macos.dmg`

These binaries are intentionally ignored by git. Keep this README and `.gitkeep` tracked so the directory exists after checkout.
