---
description: Backend — migrations, indexer, Edge Functions
globs: backend/**
alwaysApply: false
---

# Backend

- SQL migrations in `backend/supabase/migrations/` with explicit RLS
- Indexer: pure parsers + thin DB IO; Vitest fixtures when tests are requested
- Edge Functions: Deno, no `any`
- Signed download URLs via HMAC; never expose raw volume over HTTP
- Catalog soft-delete via `deleted_at`
